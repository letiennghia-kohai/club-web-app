[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash -c 'gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 wsgi:application'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
